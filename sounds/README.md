# sounds/

録音した mp3 をこのフォルダに置く。ファイル名と収録するセリフは
`car-factory-app/録音台本.md` を参照。

ファイルが無い工程は自動的に合成音（効果音のみ）で進むので、
録れたものから1つずつ追加してよい。ここに .mp3 を置いても
コミット時にリポジトリへ含めるかどうかは別途判断する
（音声ファイルは容量が大きくなりがちなので、必要なら .gitignore を検討する）。
